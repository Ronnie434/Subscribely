[functions.stripe-webhook]
verify_jwt = false

[functions.apple-webhook]
verify_jwt = false

[http]
# Allow requests without JWT authentication
# Webhooks authenticate via their own signature verification methods
# - Stripe: via Stripe signature header
# - Apple: via JWT signed payload from Apple
import_map = "../import_map.json"